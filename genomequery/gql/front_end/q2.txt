include<tables.txt>
genome NA18507;

H2=select * from READS where location>0

H3=select * from MAPJOIN genes using intervals(begin, end),H2 using intervals(location, location+length, both_mates)

#H4=select * from MAPJOIN H3 using intervals(begin, end), H2 using intervals(location, location+length) 

H5=select * from H3 where chr2=="abc" and chr1=="1234"
